apiVersion: v1
kind: Service
metadata:
  name: reading-list-api-service
  labels:
    app: reading-list-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: reading-list-api
